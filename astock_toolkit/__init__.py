"""A股选股辅助工具包，维护两个互相独立的股票池。

股票池一：底部首板（daily_scan.py 每天扫描一次）
  1. 此前30个交易日没有涨停过（首板，排除连板/接力）；
  2. 涨停当日收盘价没有超过此前30个交易日平均收盘价的1.4倍（底部）；
  3. 标注涨停后约5个交易日、回踩到涨停价位约5%左右的重点关注股，并额外标注
     近30日是否有MACD金叉+向上发散确认；
  4. 结合股票所属板块的当日热门度。

股票池二：地量后放量突破（scan_volume_surge.py 单独扫描，不要求涨停）
  1. 总市值 < 200亿；
  2. 此前30个交易日平均换手率 < 2%（地量）；
  3. 当日换手率 >= 前30日平均的2倍以上（放量）；
  4. 当日收盘价 < 前30日平均价格的1.2倍（价格还没涨出去）。

范围：沪深主板 + 创业板 + 科创板，不含北交所。

- bottom_pattern_scanner —— 股票池一：核心信号识别 + 观察池状态维护
- volume_surge_scanner   —— 股票池二：地量后放量突破识别
- macd                   —— MACD 金叉/发散计算，供股票池一的买点区间做二次确认
- sector_heat            —— 板块热门度排名 + 给信号标注所属板块
- data_source            —— AKShare 数据源封装（东方财富优先，自动降级新浪/同花顺）
- db                     —— 本地 SQLite 存储（两个股票池各自的信号表 / 行情缓存 / 运行时设置）

daily_scan.py 和 scan_volume_surge.py（项目根目录）是每天/按需运行的扫描入口；
app.py 和 generate_site.py 都只是只读展示，不含任何"刷新/更新"按钮——所有数据
更新都发生在这两个扫描脚本里。
"""
